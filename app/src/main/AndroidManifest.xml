<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileApplication" >

        <!-- Pokrećemo CreateTaskActivity kao LAUNCHER -->
        <activity android:name=".ui.CreateTaskActivity" android:exported="false" />
        <activity android:name=".ui.TaskDetailActivity"/>
        <activity android:name=".ui.CalendarActivity"/>
        <activity android:name=".ui.TaskListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- (Ako postoji stari MainActivity, ostavi ga bez intent-filtera) -->
        <!-- <activity android:name=".MainActivity" android:exported="false" /> -->

    </application>


</manifest>